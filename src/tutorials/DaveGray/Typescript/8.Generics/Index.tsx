import React from 'react'

export default function Index() {



    // const stringEcho = (arg: string): string => arg
    // // const echo = <T>(arg: T): T => arg


    // const isObject = <T extends object>(arg: T): boolean => {
    //     return typeof arg === 'object' && !Array.isArray(arg) && arg !== null
    // }





    // const isTrue = <T >(arg: T, is: boolean) => {
    //     if(Array.isArray(arg) && arg.length === 0) {
    //         return {arg, is: false}
    //     }
    //     if (isObject(arg) && Object.keys(arg as keyof T).length === 0) {
    //         return {arg, is: false}
    //     }
    //     return {arg, is: !!arg}
    //     }


























    return (
        <div>Index Generics</div>
    )
}
